function init(self)
	factory.create("#factory", vmath.vector3(5, 0.5, -5), nil, { controller = msg.url() })
end

function update(self, dt)
	
end

function on_message(self, message_id, message, sender)
	
end
